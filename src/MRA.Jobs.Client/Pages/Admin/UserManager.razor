@page "/userManager"
@inject NavigationManager NavigationManager
@inject IConfiguration Configuration 

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();
        NavigationManager.NavigateTo($"{Configuration["IdentityClient"]}/userManager");
    }

}